# FullStackChallengeFrontend


## Development server

1. Clone the remository, `cd` into it 
2. Run `npm install`
3. Run  `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.
